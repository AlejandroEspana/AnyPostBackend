# Copia este archivo a ".env" y rellena los valores reales antes de ejecutar la aplicación.
# Nunca subas tu archivo .env al repositorio.

N8N_WEBHOOK_URL=http://18.191.161.60:5678/webhook/anypost/publish

# Credenciales del servicio de video Blotato
BLOTATO_API_KEY=
BLOTATO_API_BASE_URL=https://backend.blotato.com/v2
BLOTATO_API_TEMPLATE_ID=base/pov/wakeup

# Azure Blob Storage
AZURE_BLOB_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=anypost;AccountKey=REPLACE_WITH_KEY;EndpointSuffix=core.windows.net
AZURE_BLOB_CONTAINER=uploads

# Frontend origin allowed for local development (supports localhost/127.0.0.1 on any port)
CORS_ALLOWED_ORIGINS=http://localhost:*,http://127.0.0.1:*
CORS_ALLOW_CREDENTIALS=true
